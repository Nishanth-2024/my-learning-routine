# Implement a Generic Linked List

> [!Question] Design and implement a generic linked list data structure in C#, Swift, and TypeScript. This list should store elements of any data type.

**Core Functionalities (Interface Definitions with Comments and Exceptions):**

## **C#:**

```csharp
public interface ILinkedList<T>
{
    /// <summary>Inserts a new element at the beginning of the list.</summary>
    /// <param name="element">The element to insert.</param>
    void InsertAtBeginning(T element);

    /// <summary>Inserts a new element at the end of the list.</summary>
    /// <param name="element">The element to insert.</param>
    void InsertAtEnd(T element);

    /// <summary>Inserts a new element at a specified index.</summary>
    /// <param name="index">The index at which to insert the element.</param>
    /// <param name="element">The element to insert.</param>
    /// <exception cref="ArgumentOutOfRangeException">Thrown when the index is less than 0 or greater than the list's size.</exception>
    void InsertAt(int index, T element);

    /// <summary>Removes the element from the beginning of the list.</summary>
    /// <returns>The removed element.</returns>
    /// <exception cref="InvalidOperationException">Thrown when the list is empty.</exception>
    T DeleteFromBeginning();

    /// <summary>Removes the element from the end of the list.</summary>
    /// <returns>The removed element.</returns>
    /// <exception cref="InvalidOperationException">Thrown when the list is empty.</exception>
    T DeleteFromEnd();

    /// <summary>Removes the element at a specified index.</summary>
    /// <param name="index">The index of the element to remove.</param>
    /// <returns>The removed element.</returns>
    /// <exception cref="ArgumentOutOfRangeException">Thrown when the index is less than 0 or greater than or equal to the list's size.</exception>
    /// <exception cref="InvalidOperationException">Thrown when the list is empty.</exception>
    T DeleteAt(int index);

    /// <summary>Checks if the list contains a given element.</summary>
    /// <param name="element">The element to search for.</param>
    /// <returns>True if the list contains the element, false otherwise.</returns>
    bool Contains(T element);

    /// <summary>Returns the index of the first occurrence of a given element.</summary>
    /// <param name="element">The element to search for.</param>
    /// <returns>The index of the first occurrence, or -1 if the element is not found.</returns>
    int IndexOf(T element);

    /// <summary>Returns the element at a specified index.</summary>
    /// <param name="index">The index of the element to retrieve.</param>
    /// <returns>The element at the specified index.</returns>
    /// <exception cref="ArgumentOutOfRangeException">Thrown when the index is less than 0 or greater than or equal to the list's size.</exception>
    /// <exception cref="InvalidOperationException">Thrown when the list is empty.</exception>
    T Get(int index);

    /// <summary>Returns the number of elements in the list.</summary>
    /// <returns>The number of elements.</returns>
    int Size();

    /// <summary>Checks if the list is empty.</summary>
    /// <returns>True if the list is empty, false otherwise.</returns>
    bool IsEmpty();

    /// <summary>Removes all elements from the list.</summary>
    void Clear();

    /// <summary>Returns an array representation of the list.</summary>
    /// <returns>An array containing the elements of the list.</returns>
    T[] ToArray();

    // IEnumerator<T> GetEnumerator(); // For iteration (implementation details vary)
}
```

## **Swift:**

```swift
protocol LinkedList<T> {
    /// Inserts a new element at the beginning of the list.
    func insertAtBeginning(_ element: T)

    /// Inserts a new element at the end of the list.
    func insertAtEnd(_ element: T)

    /// Inserts a new element at a specified index.
    /// - Parameters:
    ///   - index: The index at which to insert the element.
    ///   - element: The element to insert.
    /// - Throws: `IndexError` if the index is less than 0 or greater than the list's size.
    func insertAt(index: Int, element: T) throws

    /// Removes the element from the beginning of the list.
    /// - Returns: The removed element.
    /// - Throws: `EmptyListError` if the list is empty.
    func deleteFromBeginning() throws -> T

    /// Removes the element from the end of the list.
    /// - Returns: The removed element.
    /// - Throws: `EmptyListError` if the list is empty.
    func deleteFromEnd() throws -> T

    /// Removes the element at a specified index.
    /// - Parameter index: The index of the element to remove.
    /// - Returns: The removed element.
    /// - Throws: `IndexError` if the index is less than 0 or greater than or equal to the list's size.
    /// - Throws: `EmptyListError` if the list is empty.
    func deleteAt(index: Int) throws -> T

    /// Checks if the list contains a given element.
    /// - Parameter element: The element to search for.
    /// - Returns: `true` if the list contains the element, `false` otherwise.
    func contains(_ element: T) -> Bool

    /// Returns the index of the first occurrence of a given element.
    /// - Parameter element: The element to search for.
    /// - Returns: The index of the first occurrence, or `nil` if the element is not found.
    func indexOf(_ element: T) -> Int?

    /// Returns the element at a specified index.
    /// - Parameter index: The index of the element to retrieve.
    /// - Returns: The element at the specified index.
    /// - Throws: `IndexError` if the index is less than 0 or greater than or equal to the list's size.
    /// - Throws: `EmptyListError` if the list is empty.
    func get(at index: Int) throws -> T

    /// Returns the number of elements in the list.
    /// - Returns: The number of elements.
    func size() -> Int

    /// Checks if the list is empty.
    /// - Returns: `true` if the list is empty, `false` otherwise.
    func isEmpty() -> Bool

    /// Removes all elements from the list.
    func clear()

    /// Returns an array representation of the list.
    /// - Returns: An array containing the elements of the list.
    func toArray() -> [T]
}

// Example custom errors (you'll need to define these)
enum LinkedListError: Error {
    case EmptyListError
    case IndexError
}
```

## Typescript

```typescript
interface LinkedList<T> {
    /** Inserts a new element at the beginning of the list. */
    insertAtBeginning(element: T): void;

    /** Inserts a new element at the end of the list. */
    insertAtEnd(element: T): void;

    /**
     * Inserts a new element at a specified index.
     * @param index The index at which to insert the element.
     * @param element The element to insert.
     * @throws {RangeError} If the index is less than 0 or greater than the list's size.
     */
    insertAt(index: number, element: T): void;

    /**
     * Removes the element from the beginning of the list.
     * @returns The removed element.
     * @throws {Error} If the list is empty.
     */
    deleteFromBeginning(): T;

    /**
     * Removes the element from the end of the list.
     * @returns The removed element.
     * @throws {Error} If the list is empty.
     */
    deleteFromEnd(): T;

    /**
     * Removes the element at a specified index.
     * @param index The index of the element to remove.
     * @returns The removed element.
     * @throws {RangeError} If the index is less than 0 or greater than or equal to the list's size.
     * @throws {Error} If the list is empty.
     */
    deleteAt(index: number): T;

    /**
     * Checks if the list contains a given element.
     * @param element The element to search for.
     * @returns `true` if the list contains the element, `false` otherwise.
     */
    contains(element: T): boolean;

    /**
     * Returns the index of the first occurrence of a given element.
     * @param element The element to search for.
     * @returns The index of the first occurrence, or -1 if the element is not found.
     */
    indexOf(element: T): number;

    /**
     * Returns the element at a specified index.
     * @param index The index of the element to retrieve.
     * @returns The element at the specified index.
     * @throws {RangeError} If the index is less than 0 or greater than or equal to the list's size.
     * @throws {Error} If the list is empty.
     */
    get(index: number): T;

    /**
     * Returns the number of elements in the list.
     * @returns The number of elements.
     */
    size(): number;

    /**
     * Checks if the list is empty.
     * @returns `true` if the list is empty, `false` otherwise.
     */
    isEmpty(): boolean;

    /** Removes all elements from the list. */
    clear(): void;

    /**
     * Returns an array representation of the list.
     * @returns An array containing the elements of the list.
     */
    toArray(): T[];
}
```

**Implementation Details:**

* Choose either a singly or doubly linked list.
* Implement generics/type parameters correctly.
* Handle edge cases (invalid indices, empty lists).
* Implement an iterator (where applicable).

**Testing:**

Write comprehensive tests covering various scenarios.

**Deliverables:**

* Source code for each language.
* A brief report outlining implementation choices.
